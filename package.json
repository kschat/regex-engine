{
  "scripts": {
    "init": "typings install && npm run build",
    "build": "rm -rf build && tsc",
    "start": "npm run build && node index.js"
  },
  "devDependencies": {
    "typescript": "^1.8.10",
    "typings": "^1.3.1"
  }
}
